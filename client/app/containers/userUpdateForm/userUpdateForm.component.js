import template from './userUpdateForm.html';
import controller from './userUpdateForm.controller';

let userUpdateFormComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default userUpdateFormComponent;
